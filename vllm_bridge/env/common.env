# Common non-secret runtime env for all containers
HF_HOME=/hf-cache
HF_HUB_CACHE=/hf-cache/hub
TRANSFORMERS_CACHE=/hf-cache/transformers
PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True

# Keep conversion script compatible (vLLM v0 engine)
VLLM_USE_V1=0
